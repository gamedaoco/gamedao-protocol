{
  "name": "@gamedao/evm",
  "version": "3.0.0",
  "description": "GameDAO Protocol EVM contract ABIs and TypeScript types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "generate-abis": "node scripts/generateAbis.js",
    "generate-deployments": "node scripts/generateDeployments.js",
    "build": "node scripts/generateAbis.js && node scripts/generateDeployments.js && tsc",
    "dev": "node scripts/generateAbis.js && node scripts/generateDeployments.js && tsc --watch",
    "clean": "rm -rf dist",
    "prepare": "node scripts/generateAbis.js && node scripts/generateDeployments.js && tsc"
  },
  "dependencies": {
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gamedaoco/gamedao-protocol.git",
    "directory": "packages/shared"
  },
  "keywords": [
    "gamedao",
    "ethereum",
    "solidity",
    "abi",
    "types"
  ]
}
